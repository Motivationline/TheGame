namespace Script {
    import ƒ = FudgeCore;

    export interface Build {
        graph: ƒ.Graph,
        size: number,
        name: string,
        costFood: number,
        costStone: number,
    }

}